import * as HomeScreenTypes from '../../../../../src/features/home/screens/HomeScreen/types';

describe('HomeScreenTypes', () => {
  it('should have types defined', () => {
    expect(HomeScreenTypes).toBeDefined();
  });
}); 