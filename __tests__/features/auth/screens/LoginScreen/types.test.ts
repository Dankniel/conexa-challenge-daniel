import * as LoginScreenTypes from '../../../../../src/features/auth/screens/LoginScreen/types';

describe('LoginScreenTypes', () => {
  it('should have types defined', () => {
    expect(LoginScreenTypes).toBeDefined();
  });
}); 