import * as NewsDetailScreenTypes from '../../../../../src/features/news-detail/screens/NewsDetailScreen/types';

describe('NewsDetailScreenTypes', () => {
  it('should have types defined', () => {
    expect(NewsDetailScreenTypes).toBeDefined();
  });
}); 