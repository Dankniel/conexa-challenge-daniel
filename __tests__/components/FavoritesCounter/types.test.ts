import * as FavoritesCounterTypes from '../../../src/components/FavoritesCounter/types';

describe('FavoritesCounterTypes', () => {
  it('should have types defined', () => {
    expect(FavoritesCounterTypes).toBeDefined();
  });
}); 