import * as SearchBarTypes from '../../../src/components/SearchBar/types';

describe('SearchBarTypes', () => {
  it('should have types defined', () => {
    expect(SearchBarTypes).toBeDefined();
  });
}); 